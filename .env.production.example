# =============================================================================
# ENVIRONMENT VARIABLES TEMPLATE - PRODUCTION
# =============================================================================
# ⚠️  WARNING: Never commit actual production values to version control!
# Copy this file to .env and configure with your actual production values
# Use secure secret management practices for all sensitive values

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# Neon PostgreSQL connection string with pooling enabled
# Required: Yes
# Where to obtain: https://console.neon.tech/ → Select project → Connection Details → Copy connection string
# Example: postgresql://username:password@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require&pool=true
DATABASE_URL=postgresql://username:password@ep-xxx.us-east-2.aws.neon.tech/dbname?sslmode=require&pool=true

# =============================================================================
# AUTHENTICATION & SECURITY
# =============================================================================

# Server secret for Vybe integrations
# Required: Yes
# How to generate: openssl rand -base64 32
# Note: Generate a new strong, unique random string for production
VYBE_SERVER_SECRET=GENERATE_STRONG_32_BYTE_BASE64_SECRET_HERE

# =============================================================================
# INNGEST CONFIGURATION
# =============================================================================

# Inngest application identifier
# Required: Yes
# Where to obtain: https://app.inngest.com/ → Your app → Settings → App ID
INNGEST_APP_ID=your-inngest-app-id

# Inngest event key for sending events
# Required: Yes
# Production value: Obtain from Inngest dashboard → Event keys → Create new key
INNGEST_EVENT_KEY=your-production-event-key

# Inngest signing key for webhook verification (production only)
# Required: Yes (production environments)
# Where to obtain: Inngest dashboard → Signing keys → Copy production signing key
INNGEST_SIGNING_KEY=your-production-signing-key

# =============================================================================
# AI INTEGRATION
# =============================================================================

# Ollama API endpoint URL
# Required: No
# Production value: http://localhost:11434 (for VPS local Ollama installation)
# Note: Ensure Ollama is installed and running on your VPS
OLLAMA_BASE_URL=http://localhost:11434

# OpenAI API key for fallback/dual AI support
# Required: No (but recommended for production reliability)
# Where to obtain: https://platform.openai.com/api-keys → Create new secret key
OPENAI_API_KEY=your-openai-api-key

# =============================================================================
# MONITORING & ERROR TRACKING
# =============================================================================

# Sentry Data Source Name for error tracking
# Required: No (but highly recommended for production)
# Where to obtain: Sentry project → Settings → Client Keys (DSN)
SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Environment mode
# Required: Yes
# Production value: production
NODE_ENV=production

# Application display name shown in UI
# Required: No
# Production value: Your production app name
NEXT_PUBLIC_APP_NAME=AgentsFlowAI

# Vybe integrations domain URL
# Required: No
# Production value: Your production domain
NEXT_PUBLIC_VYBE_INTEGRATIONS_DOMAIN=https://yourdomain.com

# Toggle between live data and fake data
# Required: No
# Production value: true (always use live data in production)
NEXT_PUBLIC_SHOW_LIVE_DATA=true

# =============================================================================
# PRODUCTION SECURITY NOTES
# =============================================================================
# 1. Ensure all secrets are strong and unique
# 2. Never use "local", "dev", "test", or example values in production
# 3. Consider using environment-specific secret management services
# 4. Regularly rotate secrets and API keys
# 5. Monitor for unauthorized access and API usage
# 6. Keep your .env file secure and never commit to version control
# 7. Use HTTPS for all external service integrations
# 8. Implement proper IP whitelisting for database access (Neon console)
